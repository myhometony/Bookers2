

    <div class= "col-md-3">

      <h2>User info</h2>

      <table class="table">
        <%= image_tag user.get_profile_image(100,100) %>

        <tr>
          <th>name</th>
          <th><%= current_user.name %></th>
        </tr>
        <tr>
          <th>introduction</th>
        <th><%= current_user.name %></th>
        </tr>
      </table>
      <a class="btn btn-outline-secondary btn-block" href="/users/:id/edit"><i class="fa-solid fa-user-gear"></i></a>

      <h2 class="mt-3">New book</h2>
         <%= form_with model: book do |f| %>
         <div class="form-group"><label for="book_title">本のタイトル</label><%= f.text_field :title %></div>
         <div class="form-group"><label for="book_opinion">感想</label><%= f.text_area :body %></div>
         <div class="form-group"><%= f.submit 'Create Book' ,class:"btn btn-success" %></div>
         <% end %>


    </div>